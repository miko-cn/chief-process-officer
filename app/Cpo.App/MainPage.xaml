<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Cpo.App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Cpo.App"
    xmlns:vm="using:Cpo.App.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 标题与说明 -->
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="CPO 遥测 (M1)" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock
                Text="录制状态与最近事件流。M1 直接读取本地 SQLite（数据链路验证）；M2 切换为 gRPC over named pipes。"
                Style="{StaticResource CaptionTextBlockStyle}"
                Opacity="0.7"
                TextWrapping="Wrap" />
        </StackPanel>

        <!-- 状态卡片 -->
        <Border
            Grid.Row="1"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16">
            <StackPanel Spacing="8">
                <TextBlock
                    Text="{x:Bind ViewModel.StatusText, Mode=OneWay}"
                    Style="{StaticResource BodyStrongTextBlockStyle}"
                    TextWrapping="Wrap" />
                <TextBlock
                    Text="{x:Bind ViewModel.EventCountText, Mode=OneWay}"
                    Style="{StaticResource BodyTextBlockStyle}" />
                <TextBlock
                    Text="{x:Bind ViewModel.DatabasePath, Mode=OneWay}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Opacity="0.7"
                    TextWrapping="Wrap" />
                <Button
                    Command="{x:Bind ViewModel.RefreshCommand}"
                    AutomationProperties.AutomationId="RefreshButton"
                    AutomationProperties.Name="刷新事件流"
                    HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE72C;" FontSize="14" />
                        <TextBlock Text="刷新" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- 事件流列表 -->
        <ListView
            Grid.Row="2"
            x:Name="EventsList"
            ItemsSource="{x:Bind ViewModel.Events, Mode=OneWay}"
            AutomationProperties.AutomationId="EventsList"
            AutomationProperties.Name="遥测事件流">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:EventRow">
                    <Grid ColumnSpacing="12" Padding="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110" />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind Time}" Style="{StaticResource CaptionTextBlockStyle}" />
                        <TextBlock
                            Text="{x:Bind Type}"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Opacity="0.8" />
                        <TextBlock Text="{x:Bind Summary}" Style="{StaticResource BodyTextBlockStyle}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
